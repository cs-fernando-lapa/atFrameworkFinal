import angular from 'angular';
import loginalertComponent from './loginalert.component';

let loginalertModule = angular.module('loginalert', [

])

.component('loginalert', loginalertComponent)

.name;

export default loginalertModule;
